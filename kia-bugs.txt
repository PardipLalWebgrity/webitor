
** In paste layer, dont add px, because it already has px & update index for svg
styleEl.setAttribute('data-layer', copyLayer.index);
copyEl.style.left = copyLayer.left;
copyEl.style.top = copyLayer.top;